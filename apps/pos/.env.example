# POS tablet app
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
# Device identity — must be unique per tablet; registered on first boot
VITE_DEVICE_ID=pos-01
VITE_DEVICE_NAME="POS 01"
